import Redirects from "./redirects/Redirects"

export default function Navbar() {
    return(
        <nav>
            <div>Logo</div>
            <section>
                <Redirects />
            </section>
        </nav>
    )
}